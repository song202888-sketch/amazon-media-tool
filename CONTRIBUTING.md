# 参与贡献

欢迎提交问题报告和改进建议。提交代码前请遵循以下要求：

1. 不增加远程代码执行、广告、追踪或用户数据上传；
2. 不扩大到 `<all_urls>`，新增站点权限必须说明必要性；
3. 不实现 DRM、账号限制或访问控制绕过；
4. 保持 Manifest V3 兼容；
5. 运行 `npm ci`、`npm run build` 和 `npm run check`；
6. 为商品图识别、媒体捕获或权限变化补充测试；
7. 不在问题或提交中包含真实 Cookie、签名视频地址、订单信息或账号数据。

提交 Pull Request 时，请说明用户场景、权限变化、测试结果和可能的兼容性影响。
